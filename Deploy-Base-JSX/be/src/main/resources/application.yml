spring:
  application:
    name: salone-auto

  datasource:
    # In locale valgono i valori dopo i due punti. Su Render DatabaseUrl legge
    # DATABASE_URL e sovrascrive queste tre proprieta'.
    url: ${DB_URL:jdbc:postgresql://localhost:5432/salone_auto}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:admin}

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    # Niente SQL nei log: i parametri legati conterrebbero email e hash.
    show-sql: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

  # Gmail: MAIL_USERNAME e MAIL_PASSWORD (password per le app) solo da variabili
  # d'ambiente. Vuote = nessun invio (vedi MailService).
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  # Render assegna la porta in PORT.
  port: ${PORT:8080}
  shutdown: graceful
  # Dietro il proxy di Render: l'IP del client arriva in X-Forwarded-For.
  # "native" lo accetta solo da proxy interni, quindi non si puo' falsificare.
  forward-headers-strategy: native
  error:
    include-message: never
    include-stacktrace: never

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
  health:
    # Senza SMTP configurato l'health check non deve andare DOWN.
    mail:
      enabled: false

app:
  # Render imposta RENDER=true: in produzione la chiave JWT di sviluppo e' vietata.
  produzione: ${RENDER:false}
  # Tentativi ammessi prima del 429 (login in 15 minuti, il resto in un'ora).
  limiti:
    login-errati-per-email: 5
    login-per-ip: 30
    reset-per-email: 3
    reset-per-ip: 10
    registrazioni-per-ip: 10
  cors:
    # In produzione: ALLOWED_ORIGIN=https://<fe>.onrender.com (piu' valori separati da virgola).
    allowed-origins: ${ALLOWED_ORIGIN:http://localhost:5173,http://localhost:4173}
  # Indirizzo del FE: base dei link nelle mail.
  fe-url: ${FE_URL:http://localhost:5173}
  jwt:
    # Il default vale solo in locale; su Render JWT_SECRET e' generata dal blueprint.
    secret: ${JWT_SECRET:solo-per-sviluppo-locale-cambiami-0123456789abcdef}
    durata: ${JWT_DURATA:2h}
  reset-password:
    durata: 120m
  seed:
    # 50 annunci di esempio (seed/auto.json), caricati solo se la tabella auto e' vuota.
    auto: ${APP_SEED_AUTO:true}
  admin:
    email: ${ADMIN_EMAIL:}
    password: ${ADMIN_PASSWORD:}
  # Utente normale di prova: solo in locale (avvia.sh), mai su Render.
  demo:
    email: ${DEMO_EMAIL:}
    password: ${DEMO_PASSWORD:}
  mail:
    mittente: ${MAIL_FROM:${MAIL_USERNAME:no-reply@salone.local}}
    # Solo per provare in locale senza SMTP: stampa il link (mai l'indirizzo).
    log-link: ${APP_MAIL_LOG_LINK:false}
